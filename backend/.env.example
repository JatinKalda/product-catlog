# PostgreSQL connection string for local development
# See docker-compose.yml for the value used in the Docker container
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/products_db?schema=public"
